#include "lists.h"

/**
 * check_cycle - Checks if a list has a cycle
 * @list: The list to be checked
 *
 * Return: 0 if there is no cycle, otherwise 1
 */



int check_cycle(listint_t *list)
{
	int cycle = 0;

	if (!list)
		return (cycle);
/*
 *	myList = list, my_cycle = myList->next;
*/
	return (cycle);
}
